import { Container } from '@mantine/core';
import React from 'react';

// Modelo componente Loader
const Loader = () => {
  return <Container>Cargando informacion</Container>;
};

export default Loader;
